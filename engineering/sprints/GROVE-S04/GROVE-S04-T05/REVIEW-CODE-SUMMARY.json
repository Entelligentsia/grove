{
  "objective": "Re-review the config/trace-TUI + tap move to grove-explore-bin after the prior AC7 cold-target Revision Required; verify every AC independently against the on-disk tree.",
  "key_changes": [
    "AC7 blocker RESOLVED and independently reproduced: rm target/release/grove-explore, then cargo test -p grove-cst-cli --test cli triggered 'Compiling grove-explore-bin' mid-run and passed 42/42.",
    "grove_explore_bin() fix uses std::sync::Once + on-demand cargo build --locked -p grove-explore-bin, honours $CARGO, detects release/debug, bin.exists() short-circuits warm runs.",
    "Full workspace: cargo test --release --locked --workspace = 340 passed, 0 failed. Clippy --all-targets --workspace -D warnings clean (exit 0).",
    "AC1: config_tui/trace_tui/tap moved verbatim to grove-explore/src; main.rs dispatches serve/config/tap; bare invocation defaults to Serve.",
    "AC2: cli/src/main.rs Config/Tap arms print new-spelling deprecation note then exec-forward via find_explore_binary(); graceful actionable failure when binary absent.",
    "AC3: tap.rs GroveConfig read-modify-write to config.json preserving mode/harnesses; hint says 'restart grove-explore'; no user-facing explore.json strings (only migration comments).",
    "AC4: grove_mode/explore_active fields deleted from model.rs; mode badge + explore-notice row + inert guards removed from view.rs; only AC4 doc-comments remain.",
    "AC5: config_tui/mod.rs save re-reads mode AND harnesses from disk before write; config_round_trip_preserves_mode_and_harnesses asserts readback.mode == Mode::Mcp.",
    "AC6: cargo tree confirms ratatui/crossterm absent from grove-cst-cli, present in grove-explore-bin.",
    "init.rs first-run fix: replaced crate::config_tui::run with find_explore_binary() exec of grove-explore config; prior 'grove tap'/'grove serve' string advisories also addressed.",
    "Advisory (non-blocking): round-trip test re-implements the save sequence inline rather than driving the real event_loop Save arm; consider extracting a shared helper.",
    "Advisory (non-blocking): init.rs:136 first-run guard still keys on .grove/explore.json sentinel (pre-existing, acceptable during one-release deprecation window)."
  ],
  "verdict": "approved",
  "artifact_ref": "CODE_REVIEW.md"
}